@top Program { statement* }

@skip { space | LineComment }

statement[@isGroup=Statement] { Vertex | Edge }

Vertex { "vertex" Id Value }

Edge { "edge" Id Id }

@tokens {
  Id { @asciiLetter+ }

  Value { @digit+ }

  space { @whitespace+ }

  LineComment { "//" ![\n]* }
}